# ─────────────────────────────────────────────────────────────────────────────
# This project uses pyproject.toml as the single source of truth for
# dependencies. This file only exists for tooling/workflows that expect a
# requirements.txt, and it simply delegates to pyproject.
#
#   Runtime only:      pip install .
#   Runtime + dev:     pip install -e ".[dev]"
#   Via this file:     pip install -r requirements.txt   (installs the [dev] extra)
#
# If you ever need a fully pinned lockfile for reproducible deploys, GENERATE it
# instead of hand-editing:
#   pip freeze > requirements.lock.txt
# ─────────────────────────────────────────────────────────────────────────────

-e .[dev]